DB_CONNECTION=postgresql://postgres:postgres@localhost:5432/farmacia
SESSION_SECRET=una-cadena-muy-larga-de-al-menos-32-caracteres

# Usado por /api/cron (chequeo semanal de vencimientos/bajo stock, ver
# "crons" en vercel.json). En Vercel, al definir esta variable en el
# proyecto, Vercel Cron firma sus peticiones con
# "Authorization: Bearer $CRON_SECRET" automáticamente.
CRON_SECRET=otra-cadena-larga-y-aleatoria
